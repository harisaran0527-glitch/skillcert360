import Link from "next/link";
import { db } from "@/lib/db";
export default async function AdminDashboard() { const [students, active, departments] = await Promise.all([db.studentProfile.count(), db.user.count({ where: { role: "STUDENT", status: "ACTIVE" } }), db.department.count({ where: { active: true } })]); return <><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold uppercase tracking-[.16em] text-[#1e6fd9]">Admin overview</p><h1 className="mt-2 font-display text-4xl font-bold text-[#10233f]">Good morning, admin.</h1><p className="mt-2 text-slate-500">Manage student access and keep progress data trustworthy.</p></div><Link href="/admin/students" className="rounded-lg bg-[#1e6fd9] px-5 py-3 text-center font-bold text-white">Manage students</Link></div><div className="mt-10 grid gap-4 sm:grid-cols-3">{[["Total students", students], ["Active accounts", active], ["Departments", departments]].map(([label, value]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-6"><p className="text-sm text-slate-500">{label}</p><p className="mt-3 font-display text-4xl font-bold text-[#10233f]">{value}</p></div>)}</div><div className="mt-8 rounded-xl border border-slate-200 bg-white p-6"><h2 className="font-display text-xl font-bold text-[#10233f]">Phase 1 is live</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Student identities, role boundaries, departments, sections, temporary passwords, and first-login password rotation are persisted through PostgreSQL-backed Prisma models.</p></div></> }